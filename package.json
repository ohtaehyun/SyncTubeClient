{
  "name": "synctubeclient",
  "version": "1.0.2",
  "description": "Chrome Extension for synchronized YouTube watching",
  "scripts": {
    "build": "npm run build:release",
    "build:release": "node scripts/bundle.js --mode=production",
    "build:local": "node scripts/bundle.js --mode=development",
    "dev": "node scripts/bundle.js --mode=development --watch",
    "build:tsc": "node scripts/build.js"
  },
  "keywords": [
    "youtube",
    "sync",
    "watch-together"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.269",
    "@types/node": "^25.0.10",
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "socket.io-client": "^4.8.3"
  }
}
